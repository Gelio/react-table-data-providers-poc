/**
 * TODO:
 * 1. Have a cache for neighboring pages
 * 2. When calling the base `getData`, augment the `params` (`pageSize` and `page`) to include padding
 * 3. Bust the cache somehow
 */

export const dataGetterWithCache = null;
